# `profiles`

**Responsibility:** Loading, merging, and validating model profiles, agent profiles, and project overrides; the catalog; the capability bundles file.

**Mutable by the evolve loop?** Content yes; the loader no.

See the crate table in `docs/agent-harness-dev-plan.md` §3.1 and the dependency
DAG in `crates/README.md`. This crate is a stub until the milestone that fills it
in (see `docs/IMPLEMENTATION_PLAN.md`).
