//! `kernel` — Agent loop, typed State, Tool trait, Middleware trait, event log, checkpoints, suspend/resume.
//!
//! Stub crate (P0.0). Exports nothing yet. See `crates/kernel/README.md` for
//! its responsibility and `docs/IMPLEMENTATION_PLAN.md` for when it fills in.
